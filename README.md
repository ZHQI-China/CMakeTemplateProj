# CMake 模板项目

- 下载 vs 编译器:https://visualstudio.microsoft.com/zh-hans/downloads/
  > 可以下载整个vs IDE，也可以只下载 vs 生成工具

- 下载项目，用vscode打开
  > 插件：C/C++ Extension Pack(其中包括了CMake);Chinese

  > 设置中搜索 cmake status bar->visible

  > 设置中搜索 字体

- 点击底部的cmake status bar
  > x64 Debug/Release

  > test00-env

  > 生成
  
  > Run
